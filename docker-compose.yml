services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - ./backend/.env
    restart: always

  admin:
    build:
      context: ./admin
      dockerfile: Dockerfile
    ports:
      - "5173:80"
    restart: always

  customer:
    build:
      context: ./customer
      dockerfile: Dockerfile
    ports:
      - "5174:80"
    restart: always
