<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title><%= appName %> — Verify your email</title>
  <style>
    /* Simple, email-safe styles */
    body { margin:0; padding:0; background-color:#f4f6f8; font-family: Arial, sans-serif; color:#333; }
    .container { max-width:600px; margin:32px auto; background:#ffffff; border-radius:8px; overflow:hidden; box-shadow:0 4px 18px rgba(0,0,0,0.06); }
    .header { background:linear-gradient(90deg,#4f46e5,#06b6d4); padding:28px 24px; color:#fff; text-align:center; }
    .header h1 { margin:0; font-size:20px; font-weight:700; letter-spacing:0.2px; }
    .content { padding:24px; line-height:1.45; font-size:15px; color:#333; }
    .greeting { margin-bottom:16px; font-size:16px; }
    .button-wrap { text-align:center; margin:22px 0; }
    .cta { display:inline-block; padding:12px 20px; border-radius:6px; text-decoration:none; font-weight:600; background:#111827; color:#fff; }
    .muted { color:#6b7280; font-size:13px; margin-top:8px; }
    .small { font-size:13px; color:#6b7280; }
    .footer { background:#fafafa; padding:18px 24px; text-align:center; font-size:13px; color:#6b7280; }
    .link { word-break:break-all; color:#2563eb; text-decoration:none; }
    @media (max-width:420px){ .header h1{font-size:18px} .content{padding:18px} }
  </style>
</head>
<body>
  <div class="container" role="article" aria-label="<%= appName %> Account Verification">
    <div class="header">
      <h1><%= appName %></h1>
    </div>

    <div class="content">
      <p class="greeting">Hi <%= userName || 'there' %>,</p>

      <p>Thanks for creating an account with <strong><%= appName %></strong>. Please confirm your email address by clicking the button below. This helps us keep your account secure and ensures you receive important notifications.</p>

      <div class="button-wrap">
        <a href="<%= verificationLink %>" class="cta" target="_blank" rel="noopener">Verify my email</a>
      </div>

      <p class="muted">If the button above doesn't work, copy and paste the link below into your browser:</p>
      <p class="small"><a class="link" href="<%= verificationLink %>" target="_blank" rel="noopener"><%= verificationLink %></a></p>

      <p class="small">This link will expire in <strong><%= expiresIn || '24 hours' %></strong>.</p>

      <hr style="border:none;border-top:1px solid #eee;margin:18px 0" />

      <p class="small">If you did not create an account with <%= appName %>, you can safely ignore this email — no action is required.</p>

      <p style="margin-top:10px" class="small">Need help? Contact us at <a href="mailto:<%= supportEmail %>" class="link"><%= supportEmail %></a></p>
    </div>

    <div class="footer">
      <div><strong><%= appName %></strong></div>
      <div class="small">© <%= new Date().getFullYear() %> <%= appName %>. All rights reserved.</div>
    </div>
  </div>
</body>
</html>
