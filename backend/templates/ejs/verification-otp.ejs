<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title><%= appName %> — Verify your account</title>
  <style>
    body { 
      margin: 0; 
      padding: 0; 
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      background-color: #f6f9fc;
      color: #1a1a1a;
    }
    .email-container { 
      max-width: 600px; 
      margin: 40px auto; 
      background: #ffffff;
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0 2px 8px rgba(0,0,0,0.05);
    }
    .header { 
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      padding: 40px 30px;
      text-align: center;
    }
    .header h1 { 
      margin: 0;
      color: #ffffff;
      font-size: 28px;
      font-weight: 600;
      letter-spacing: -0.5px;
    }
    .content { 
      padding: 40px 30px;
    }
    .greeting { 
      font-size: 18px;
      color: #1a1a1a;
      margin: 0 0 20px 0;
      font-weight: 500;
    }
    .message { 
      font-size: 15px;
      line-height: 1.6;
      color: #4a5568;
      margin: 0 0 30px 0;
    }
    .otp-container {
      background: #f7fafc;
      border: 2px solid #e2e8f0;
      border-radius: 8px;
      padding: 30px;
      text-align: center;
      margin: 30px 0;
    }
    .otp-label {
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: #718096;
      margin-bottom: 12px;
      font-weight: 600;
    }
    .otp-code {
      font-size: 36px;
      font-weight: 700;
      letter-spacing: 10px;
      color: #2d3748;
      font-family: 'Courier New', monospace;
      margin: 10px 0;
    }
    .otp-expiry {
      font-size: 13px;
      color: #718096;
      margin-top: 12px;
    }
    .warning-box {
      background: #fffbeb;
      border-left: 4px solid #f59e0b;
      padding: 16px 20px;
      margin: 25px 0;
      border-radius: 4px;
    }
    .warning-text {
      font-size: 13px;
      color: #92400e;
      margin: 0;
      line-height: 1.5;
    }
    .warning-text strong {
      font-weight: 600;
    }
    .divider {
      border: none;
      border-top: 1px solid #e2e8f0;
      margin: 30px 0;
    }
    .footer-text {
      font-size: 13px;
      color: #718096;
      line-height: 1.6;
      margin: 15px 0;
    }
    .footer {
      background: #f7fafc;
      padding: 30px;
      text-align: center;
      border-top: 1px solid #e2e8f0;
    }
    .footer-brand {
      font-size: 16px;
      font-weight: 600;
      color: #2d3748;
      margin-bottom: 8px;
    }
    .footer-copyright {
      font-size: 12px;
      color: #a0aec0;
    }
    .link {
      color: #667eea;
      text-decoration: none;
    }
    .link:hover {
      text-decoration: underline;
    }
    @media (max-width: 600px) {
      .email-container {
        margin: 20px;
      }
      .header {
        padding: 30px 20px;
      }
      .header h1 {
        font-size: 24px;
      }
      .content {
        padding: 30px 20px;
      }
      .otp-code {
        font-size: 32px;
        letter-spacing: 8px;
      }
    }
  </style>
</head>
<body>
  <div class="email-container">
    <div class="header">
      <h1><%= appName %></h1>
    </div>

    <div class="content">
      <p class="greeting">Hi <%= userName || 'there' %>,</p>

      <p class="message">
        We received a request to verify your account. To complete the verification process, please use the One-Time Password (OTP) below:
      </p>

      <div class="otp-container">
        <div class="otp-label">Your Verification Code</div>
        <div class="otp-code"><%= otp %></div>
        <div class="otp-expiry">This code expires in <strong><%= expiresIn || '5 minutes' %></strong></div>
      </div>

      <div class="warning-box">
        <p class="warning-text">
          <strong>⚠️ Security Notice:</strong> This code is confidential. <%= appName %> will never ask you to share this code via phone, email, or any other medium. If you didn't request this code, please ignore this email.
        </p>
      </div>

      <hr class="divider" />

      <p class="footer-text">
        If you have any questions or need assistance, please don't hesitate to contact our support team at <a href="mailto:<%= supportEmail %>" class="link"><%= supportEmail %></a>
      </p>

      <p class="footer-text">
        If you didn't request this verification code, you can safely ignore this email.
      </p>
    </div>

    <div class="footer">
      <div class="footer-brand"><%= appName %></div>
      <div class="footer-copyright">© <%= new Date().getFullYear() %> <%= appName %>. All rights reserved.</div>
    </div>
  </div>
</body>
</html>
